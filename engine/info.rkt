#lang info

(define collection "engine")
(define deps '("base"))
(define build-deps '("rackunit-lib"))
(define scribblings '())
(define pkg-desc "zchemer grading engine: sandboxed execution, static checks, and MVP track workspaces")
(define version "0.0")
